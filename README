command to move all files to build: find . -type f -exec mv "{} +" build/

Project Description: 
@ Project Write Kernel (module) Driver
@ Date: 1.1.2020
@ Author: Guy Sudai

Description: This kernel driver acts as a journal and can be written to or read from. 

Install: 

1. Use "sudo ./remove.sh" script in the build directory in order to remove the current running journal.
2. Use "sudo ./create.sh" in order to create the new driver into kernel
3. Get major number from dmesg with 'watch -c "dmesg | tail -n 15"'
4. Launch "sudo ./create_dev.sh" in order to create the new device driver at /dev/journal
5. Use it with open,read,write,release.

