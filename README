command to move all files to build: find . -type f -exec mv "{} +" build/

Project Description: 
@ Project Write Kernel (module) Driver
@ Date: 1.1.2020
@ Author: Guy Sudai

Description: This kernel driver acts as a journal and can be written to or read from. 

Install: 

1. Use the ./remove.sh script in the build directory in order to remove the current running journal.
2. Use the ./create.sh in order to create the new driver into kernel
3. Use 
$ sudo mknod /dev/journal MAJOR_NUM 0 
in order to create a new device in /dev/journal.


